{% extends 'base.html.twig' %}

{% block title %}ProductoServicio{% endblock %}

{% block body %}
    <h1>ProductoServicio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producto_servicio.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ include('producto_servicio/_producto_servicio_descripcion_form.html.twig') }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ producto_servicio.codigo }}</td>
            </tr>
            <tr>
                <th>ProductoServicio</th>
                <td>{{ producto_servicio.productoServicio }}</td>
            </tr>
            <tr>
                <th>Rubro</th>
                <td>{{ producto_servicio.rubro.rubro }}</td>
            </tr>
            <tr>
                <th>Unidad de medida</th>
                <td>{{ producto_servicio.unidadMedida.unidadMedida }}</td>
            </tr>
            <tr>
                <th>Condición de IVA</th>
                <td>{{ producto_servicio.condicionIva.condicionIva }}</td>
            </tr>
            <tr>
                <th>PrecioBrutoUnitario</th>
                <td>{{ producto_servicio.precioBrutoUnitario }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-1">
        <a class="btn btn-info" href="{{ path('app_producto_servicio_index') }}">back to list</a>

        <a class="btn btn-primary" href="{{ path('app_producto_servicio_edit', {'id': producto_servicio.id}) }}">edit</a>
    </div>

    {{ include('producto_servicio/_delete_form.html.twig') }}
{% endblock %}
